项目依赖如下
[package]
name = "xlsx-slx"
version = "0.1.0"
edition = "2024"

[dependencies]
calamine = "0.31.0"
sqlx = { version = "0.8.6", features = ["runtime-tokio-rustls", "sqlite"] }
tokio = { version = "1.37.0", features = ["full"] }
anyhow = "1.0.82"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

请遵守以下要求:
1. rust版本为1.91 
2. 使用gemini-2.5-pro模型回答所有问题
3. 每一步都要有精细化的错误控制,以用于生产环境
4. 只需要给我代码，不要执行任何操作    
5. 现在是2025年11月,请使用最新版本的库

实现：
1.提示用户输入xlsx文件所在的文件夹，然后将文件夹中所有xlsx文件内容存入sqlite数据库，使用xlsx文件中支付帐号
作为数据库的表名，将第一个xlsx文件的第一行作为列名。
2.中文列名对数据库是否有影响？如果有，那么如何解决？
3.每一步都要有精细化的错误控制
4.只需要给我代码，不要执行任何操作           
   

11.14 设计

