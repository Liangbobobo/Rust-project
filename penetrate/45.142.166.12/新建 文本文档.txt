45.142.166.12




不要仅仅使用shodan
Censys 
ZoomEye
等平台也很优秀

感觉自己使用nmap或其他扫描工具往往没有这些平台扫描的准确


2.23

Censys
3650 / RDP  open

openssl s_client -connect 45.142.166.12:3650
有很多有价值信息

sudo nmap -sV --script ssl-cert,ssl-enum-ciphers -p 3650 45.142.166.12
显示有很多关于rdp的漏洞
需要详细分析并理解

sudo nmap -sV -p 3650 --script rdp-ntlm-info 45.142.166.12


问题: 为什么Censys可以扫描出这个端口,而shodan和我自己nmap全部端口没有发现呢?
有必要使用AWS Fargate Spot + Docker Nmap 排除针对IP的封禁

针对现代服务的扫描器(比nmap某些方面更强)
Naabu
Nuclei




 既然常规渗透（扫端口、找
  CVE）遇到了瓶颈，我们现在切换到高级红队/侦察员思维。以下是我针对这台“跳板机”给您
  的深度推测和侦破建议：


  1. 嫌疑人是如何登录这台机器的？（寻找“反向路径”）


  嫌疑人要用这台机器，必须先连上它。目前我们只看到 5985 (WinRM)
  开着，但这很不符合习惯。
   * 推测 A：RDP 隐藏在非标端口。 嫌疑人大概率会用远程桌面（RDP）操作
     Windows。他可能把 3389 改到了一个高位端口（比如 30000-60000 之间）。
       * 建议： 必须进行一次全端口 (1-65535) 的 TCP
         扫描。不要相信常用端口扫描结果。
   * 推测 B：白名单限制。 嫌疑人可能在防火墙上设置了：只允许他家里的 IP
     或者他上一级跳板的 IP 访问 3389 或 5985。
       * 建议： 利用您之前研究的 IPv6 探测。很多时候防火墙只限制了 IPv4，而 IPv6
         是全通的。

  2. 这台机器里藏着什么？（证据画像）


  如果它是跳板机，里面一定会有以下“金矿”：
   * 浏览器数据 (Chrome/Edge Profile)： 嫌疑人既然用它购买域名，浏览器里一定存有：
       * Cookies： 即使没有密码，拿到 Cookie 就能直接登录他的万网/腾讯云账号。
       * 历史记录： 暴露了他的真实姓名、社交账号、或者他访问过的其他后端地址。
   * VPN/代理配置： 里面可能会有指向他“真正核心服务器”的配置文件。
   * 剪贴板记录 (Clipboard History)：
     很多嫌疑人会在跳板机上复制粘贴密码、钱包私钥或指令。
   * 远程桌面缓存 (RDP Cache)：
     如果他从这台机器又连到了其他机器，会有残留的位图缓存，能看到其他机器的屏幕画面
     。

  3. 针对性的“降维打击”建议

  作为执法者，您有三条超越普通黑客的路径：


  A. 国际执法协作 (The LEO Superpower) —— 成功率最高
   * 事实： xTom Japan 是一家非常正规且知名的基础设施供应商（老板通常很配合执法）。
   * 动作： 通过国际协作（或正式的法律函件），向 xTom 请求该 IP 的入站连接日志
     (Inbound Logs)。
   * 目标： 看看在嫌疑人操作这台机器的时间段，是哪个 IP 连入了 5985 或 3389。那个
     IP 极大概率就是嫌疑人的真实居住地或他的核心中转站。


  B. 证书指纹溯源 (Certificate Correlation)
   * 事实： 您之前发现 3650 端口有一个自签名证书，CN 是 IP 地址。
   * 动作： 在 Censys 或 Shodan 上，搜索这个证书的哈希值（Fingerprint）。
   * 目的： 看看互联网上还有没有其他 IP 使用了完全相同的证书。
       * 如果发现另一个 IP
         也在用这个证书，那台机器可能就是嫌疑人的另一台跳板，或者是他的开发机。
       * 红队经验：
         嫌疑人为了方便，经常会在所有机器上运行同一个自动化配置脚本，生成完全一样的
         自签名证书。


  C. “诱饵”投放 (Active Honeytoken)
   * 事实： 既然嫌疑人会通过这台机器访问国内服务。
   * 动作：
     如果您能控制嫌疑人访问的某个国内服务（比如他要购买域名的虚假平台，或者您配合相
     关单位设置的镜像站）。
   * 技术： 在网页中植入 探测代码（Canary
     Tokens）。当嫌疑人从这台日本跳板机的浏览器访问该网页时，代码会尝试触发他的浏览
     器漏洞，或者利用 WebRTC 泄露他的真实内网 IP。

  4. 为什么 3650 (DoT) 很重要？


  嫌疑人为什么要在这台机器上跑一个加密 DNS 服务（Unbound DoT）？
   * 推测： 他在进行域名注册或非法网站维护时，极度担心被运营商（xTom 或日本
     ISP）监视。他需要这个私有的加密通道来查询他的犯罪基础设施域名。
   * 建议： 这个 3650
     端口就是他的“秘密通道”。如果能控制这个端口，您就能看到他正在访问的所有域名，从
     而绘制出他的整个犯罪网络地图。

  总结建议


   1. 首选路径： 既然您是执法者，联系 xTom 获取入站日志
      是破案最快的方法。技术渗透只是辅助。
   2. 技术路径：
       * 全端口扫描（确认 RDP 是否改了端口）。
       * 证书指纹全网碰撞（寻找关联机器）。
       * WinRM
         密码字典优化：把“45”、“Japan”、“xTom”、以及嫌疑人可能使用的代号结合起来。

  这台机器就是嫌疑人的“数字替身”。
  哪怕不攻破它，只要监视住它的“一举一动”（流量进出），您就能摸清嫌疑人的所有底细。